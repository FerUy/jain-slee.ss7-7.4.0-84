<?xml version="1.0" encoding="UTF-8"?>

<project name="DUManagement" default="deploy">

	<!-- SYSTEM RELATED -->
	<property environment="system" />
	<property name="jboss.home" value="${system.JBOSS_HOME}" />
	<property name="jboss.deploy" value="${jboss.home}/standalone/deployments" />

	<!-- AUTO GENERATED BY MAVEN DU PLUGIN -->
	

	<property name="du.filename" value="restcomm-slee-ra-map-du-7.4.0-84.jar" />

	<target name="deploy">
		<copy overwrite="true"
			file="${du.filename}"
			todir="${jboss.deploy}" />
	</target>

	<target name="undeploy">
		<delete file="${jboss.deploy}/${du.filename}" />
	</target>

</project>
